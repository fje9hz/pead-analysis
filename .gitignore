# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Raw WRDS files — proprietary, never commit
data/raw/

# Row-level processed data — not committed (licensed event data)
# features.csv is derived/computed (not raw WRDS) so it IS committed
data/processed/car_panel.csv
data/processed/surprise.csv
data/processed/model.joblib

# Safe aggregate outputs — committed (no row-level data)
# data/processed/aggregates/ is tracked

# Env
.env
*.env

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Pytest
.pytest_cache/
.coverage
htmlcov/
